{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "setup_local_webhook",
            "command": "${workspaceFolder}/scripts/setup_local_webhook.sh",
            "problemMatcher": []
        }
    ]
}